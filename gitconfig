[user]
    name = Kevin Qiu
    email = kevin@idempotent.ca
[color]
    diff = auto
    status = auto
    branch = auto
[core]
    autocrlf = false 
[alias]
    st = status
    sts = status -s
    m = merge --no-ff
    com = checkout master
    co = checkout
    cob = checkout -b
    pom = pull origin master
    po = pull origin
    lo = log
[merge]
    tool = meld
[mergetool "p4merge"]
    cmd = p4merge "$BASE" "$LOCAL" "$REMOTE" "$MERGED"
    trustExitCode = false
[mergetool "meld"]
    path = /usr/bin/meld
[push]
	default = simple
